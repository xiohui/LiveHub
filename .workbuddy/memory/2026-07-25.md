# 2026-07-25

## LiveHub 优化 + 部署

- 全面审计 PWA，完成 4 项优化：
  1. OpenCode 手册本地化（去外链 liamamilin.github.io，引入 manualData 弹窗渲染，旧外链数据自动迁移）
  2. manifest.json start_url/scope 改为 "."（适配 GitHub Pages 子路径 /LiveHub/）
  3. 补全 .manual-content 代码块/表格样式
  4. SW 缓存升级 v7→v8（index.html + sw.js 同步）
- 新增 .github/workflows/deploy.yml（GitHub Actions 自动部署）+ .nojekyll（关 Jekyll）
- git commit d4ddb49（8 files, +138）成功
- push 失败：沙箱无 GitHub 凭据（无 gh/SSH，凭据助手需交互）。待用户在本地 `git push origin main`，并到 Settings→Pages 把 Source 改为 GitHub Actions 后自动上线。
